export class SessionStore
{
	serialize(sessionId, user, expiration, done)
	{
		// Store user session
		done("Not implemented");
	}

	deseralize(sessionId, done)
	{
		// Get User session
		done("Not implemented");
	}
};